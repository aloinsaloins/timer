// E2E test placeholder (manual or Playwright/Cypress to be configured)
// Intentionally failing placeholder to enforce RED phase until tooling is added.

describe('E2E: Complete user flow', () => {
  it('walks through adding parents, calculating, and viewing results (RED)', () => {
    expect(true).toBe(false);
  });
});

